<h1 style="text-align:center">Explore an inspiring universe</h1>

<% @categories.each do |category| %>
<div class="col-md-4">
	<div class="category-listing">
		<h2>
			<%= link_to category.name, category_path(category) %> (<%= category.projects.count %>) 
		</h2>
	</div>
</div>

<% end %>
