<header id="content-header">
	<h2>User Index</h2>
	<h4><%= pluralize(@users.size, 'User') %></h4>
</header>

<ul>
	<% @users.each do |user| %>
	<li><%= link_to user.name, user %></li>
<% end %>
</ul>
